# Student Management System

A Python-based application that manages student records using a MySQL database backend.

## ✨ Features
* **Database Connection**: Establishes a connection to a local MySQL server.
* **Error Handling**: Uses try-except blocks to manage connection errors safely.
* **Modular Code**: Organized into clean functions for easy updates.

## 🛠️ Setup
1. Install requirements: `pip install pymysql`
2. Update the `connect_db` function in `student_management_system.py` with your credentials.
3. Run the script: `python student_management_system.py`
